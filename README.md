# hx-core
Stop rewriting the agent loop. A small Python library with pluggable providers, memory drivers, and swappable context-engineering strategies — improve compaction once, every agent inherits it.
